async function fetchAllRecords(baseId,table,view){let records=[],offset="";do{let url=`https://api.airtable.com/v0/${baseId}/${encodeURIComponent(table)}?view=${encodeURIComponent(view)}`;if(offset)url+=`&offset=${offset}`;const r=await fetch(url,{headers:{Authorization:`Bearer ${CONFIG.TOKEN}`}});const d=await r.json();records=records.concat(d.records||[]);offset=d.offset;}while(offset);return records;}